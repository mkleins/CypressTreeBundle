<a href="#" {% if node.related_referential_id and node.is_related_invisible!= true %}id="{{ node.related_referential_id }}"{% endif %} >{{ ref_before == 1 ? node.reference~' - ' : '' }}{{ node.name is defined ? node.name : '' }}</a>
<a href="#" {% if node.related_referential_id %} id="{{ node.related_referential_id }}"{% endif %} {% if node.id %} title="{{ node.id }}"{% endif %}>{{ ref_before == 1 ? node.reference~' - ' : '' }}{{ node.name is defined ? node.name : '' }}</a>